name: Phase377 Advance

on:
  push:
    branches: [novel-distill-v1]
    paths:
      - 'tools/phase376_to_377.py'
      - '.github/workflows/phase377_advance.yml'

permissions:
  contents: write

jobs:
  advance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Patch authoritative state
        run: python tools/phase376_to_377.py
      - name: Commit Phase377 state
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add state/project_state.json state/continuity/LATEST_CHECKPOINT.json
          git diff --cached --quiet || git commit -m 'phase377: advance to AI-smell root-cause audit'
          git push
