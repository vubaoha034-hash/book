# Source Register

当前还没有导入书籍。

把书籍或写作资料放入本地 `sources/books/` 或 `sources/notes/` 后，运行：

```bash
python3 scripts/ingest.py sources/books sources/notes
```

导入成功后，这里会记录文件名、格式、字数、提取状态和对应的 `_extracted` 文件。
